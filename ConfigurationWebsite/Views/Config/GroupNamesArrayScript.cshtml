@model IEnumerable<Roblox.ConfigurationWebsite.Models.Configuration.ConfigGroupModel>

<script type="text/javascript">
    var groupNames = [
        @foreach (var configGroup in Model)
        {
            @:"@configGroup.Name",
        }
    ];
</script>